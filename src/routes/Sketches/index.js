import * as React from 'react';
import * as THREE from 'three';
import { Canvas } from 'react-three-fiber';
import Stars from './Stars';
import PulsingSphere from './PulsingSphere';

const Sketches = () => {
  return <PulsingSphere />;
};

export default Sketches;
